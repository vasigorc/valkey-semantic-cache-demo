{
  "name": "airline-helpdesk-backend",
  "version": "1.0.0",
  "description": "Backend for Airline Helpdesk Chat App with Valkey semantic caching",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "valkey",
    "semantic-cache",
    "aws-bedrock",
    "vector-search"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0",
    "@valkey/valkey-glide": "^2.1.2",
    "@aws-sdk/client-bedrock-runtime": "^3.926.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "@types/node": "^22.9.1",
    "@types/express": "^5.0.0",
    "ts-node-dev": "^2.0.0"
  }
}
